# Hangman
A very simple hangman game written in C++  

Requirements: g++  

To compile this game run `make` in the source directory.  
If you want to install the game, run `make install` in the source directory.  
To uninstall run `make uninstall`.